[build]
  publish = "dist"
  command = "pnpm install --frozen-lockfile && pnpm build"

# Environment variables for build
[build.environment]
  # Allow Sharp to be installed properly
  SHARP_IGNORE_GLOBAL_LIBVIPS = "1"
  # Enable pnpm to run build scripts
  ENABLE_PNPM_BUILD_SCRIPTS = "true"
  # Force pnpm to allow build scripts
  PNPM_ENABLE_PRE_POST_SCRIPTS = "true"

# Netlify Forms configuration
[forms]
  enabled = true

# Security headers
# Note: Redirects for SPA routing not needed for Astro static sites

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin" 